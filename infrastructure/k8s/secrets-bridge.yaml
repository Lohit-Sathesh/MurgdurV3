# This Secret is a TEMPLATE. Do not commit real values to git.
#
# In production, prefer creating this Secret out-of-band (kubectl, a sealed-secrets
# controller, or your cloud provider's secrets manager + External Secrets Operator)
# rather than applying this file directly. To create/update it manually:
#
#   kubectl create secret generic murgdur-secrets -n murgdur \
#     --from-literal=DATABASE_URL='postgresql://user:pass@host:5432/murgdurV2?schema=public&pgbouncer=true' \
#     --from-literal=JWT_SECRET='...' \
#     --from-literal=JWT_REFRESH_SECRET='...' \
#     --from-literal=NEXTAUTH_SECRET='...' \
#     --from-literal=R2_ACCESS_KEY_ID='...' \
#     --from-literal=R2_SECRET_ACCESS_KEY='...' \
#     --from-literal=MEILISEARCH_API_KEY='...' \
#     --from-literal=NEXT_PUBLIC_MEILISEARCH_SEARCH_KEY='...' \
#     --from-literal=SMTP_USER='...' \
#     --from-literal=SMTP_PASS='...' \
#     --from-literal=RAZORPAY_KEY_ID='...' \
#     --from-literal=RAZORPAY_KEY_SECRET='...' \
#     --from-literal=NEXT_PUBLIC_RAZORPAY_KEY_ID='...' \
#     --from-literal=SENTRY_DSN='...' \
#     --from-literal=NEXT_PUBLIC_SENTRY_DSN='...' \
#     --dry-run=client -o yaml | kubectl apply -f -
apiVersion: v1
kind: Secret
metadata:
  name: murgdur-secrets
  namespace: murgdur
type: Opaque
stringData:
  DATABASE_URL: replace-me
  # Only needed if using pgbouncer-deployment.yaml (split out from DATABASE_URL):
  DB_HOST: replace-me
  DB_PORT: "5432"
  DB_USER: replace-me
  DB_PASSWORD: replace-me
  DB_NAME: murgdurV2
  JWT_SECRET: replace-me
  JWT_REFRESH_SECRET: replace-me
  NEXTAUTH_SECRET: replace-me
  R2_ACCESS_KEY_ID: replace-me
  R2_SECRET_ACCESS_KEY: replace-me
  MEILISEARCH_API_KEY: replace-me
  NEXT_PUBLIC_MEILISEARCH_SEARCH_KEY: replace-me
  SMTP_USER: replace-me
  SMTP_PASS: replace-me
  RAZORPAY_KEY_ID: replace-me
  RAZORPAY_KEY_SECRET: replace-me
  NEXT_PUBLIC_RAZORPAY_KEY_ID: replace-me
  SENTRY_DSN: replace-me
  NEXT_PUBLIC_SENTRY_DSN: replace-me
  GRAFANA_ADMIN_PASSWORD: replace-me
